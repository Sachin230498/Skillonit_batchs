// State  => kind of varible
// State is Data that belongs to a components and can change over the time

// Props => properties
// Props are data passed from one components to another(parent to child)
// Props can read only
// you can not change in props


const Prob1 = ()=>{
return (
  <div>
    <h1>Hello Props</h1>
    <h2>
      Props are data passed from one components to another(parent to child)
    </h2>
  </div>
);
}

export default Prob1;